class Capacitor():
	""" A simple model for capacitor"""
	
	def __init__(self, value, pn, ref):
		self.value = value
		self.pn = pn
		self.ref = ref
	def show_value():
		return [self.value, str(self.pn), self.ref]
			
